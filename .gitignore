.idea
cromwell-executions
cromwell-workflow-logs
**.DS_Store**
AGENTS.md
wdl/codegen/__pycache__/
# python test suite
# The synthetic ground-truth cohort under python/tests/synthetic/data/ is fully
# regenerable (the test fixtures rebuild it into a temp dir each run) and is NOT
# committed. Regenerate locally for inspection — see python/TESTING.md.
python/tests/synthetic/data/
.venv-test/
.pytest_cache/
python/**/__pycache__/
python/.coverage
python/tests/**/.pytest_cache/

# MkDocs build output
/site/
